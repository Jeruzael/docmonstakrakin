The Batch 1.5 final correction findings have been resolved and verified across the codebase:
Key Findings & Corrections Implemented
Untracked Machine Token Artifact: Removed the machine token from .docmonstakrakin/ to prevent sensitive host token leakage into source control, while retaining the root .gitignore pattern.
Deterministic SecretStore Configuration: Standardized the active runtime SecretStore directory to .secrets/ (store.enc, store.salt, and .secrets/.machine_token).
Historical Split-Layout Compatibility: Extended EncryptedStoreConfig and EncryptedFileSecretStore to support legacyMachineTokenPath (configured to .docmonstakrakin/.machine_token). If an existing envelope was created under the previous layout, it discovers the legacy token in read-only mode, decrypts the store, executes the authenticated re-key ceremony under the new primary master passphrase, and records structured migration status (source: LEGACY_MACHINE_TOKEN, target: CONFIGURED_MASTER_KEY).
Recovery Candidate Semantics: Invariants enforced in ensureInitialized():
Priority order: Configured primary key 
 existing current machine token 
 explicit legacy machine token.
Identical candidate values deduplicated.
No token files created during recovery.
Fails closed with safe key-source diagnostic tags if no candidate authenticates.
Historical Split-Layout Regression Suite: Added Test 16 (Scenario Historical Split Layout) to the verification suite in server/secrets/testSecretStore.ts. It creates an envelope using the historical layout, migrates and re-keys it under a new primary passphrase, deletes the legacy token, and reloads with the primary key alone. All 16 direct SecretStore tests pass.
Fail-Closed Secret Resolution: Removed broad try/catch error swallowing in server.ts and server/secrets/secretStoreFactory.ts. If the secure store encounters authentication, decryption, or tamper failures, the system fails closed rather than silently falling back to process.env.
Restored Port Behavior: Reverted port resolution in server.ts to Number(process.env.PORT || 3000).
Environment Documentation: Restored descriptive, safe comments in .env.example for PORT, DOCMONSTAKRAKIN_MASTER_KEY, DMK_HUMAN_REVIEWERS, and GEMINI_API_KEY.
Control Documentation Reconciliation: Updated docs/00_control/PROJECT_STATE.md and docs/00_control/LAST_HANDOFF.md to accurately reflect the active drawer requirement architecture, SecretStore layout, fail-closed resolution, historical migration support, exact evidence paths (docs/07_verification/rc-regression/results.json), and clear merge handoff instructions.
Verification Results
Complete Regression Suite: npm run test:complete completed with 22/22 suites passed and 310/310 assertions passed (0 failed, 0 skipped).
Static Typing & Linter: npm run lint (tsc --noEmit) succeeded with 0 errors.
Production Build: compile_applet (npm run build) succeeded without warnings or errors.
Governance Status: Gate 7 has not been executed; Batch 2 has not been started.