Implemented both corrections. **DMK-194 remains `VERIFICATION_PENDING`.**

- [App.tsx](C:/Users/HomePC/dev/docmonstakrakin/src/App.tsx:136) now preserves drawers, filters, tabs and questionnaire state during same-project refreshes. Failed refreshes hide stale content; retry reloads data without repeating the mutation.
- [PackageTransferModal.tsx](C:/Users/HomePC/dev/docmonstakrakin/src/components/PackageTransferModal.tsx:19) supports global import without an active project. Export requires a ready matching project and is guarded in both the UI and handler.
- Added real-browser tests and dev-only Playwright with lockfile updates. Existing work was preserved; branch/head remain unverified under the no-Git restriction.

Both defects were reproduced before their respective fixes.

| Verification | Result |
|---|---|
| `test:projects-workspace` | Exit 0; 14 checks |
| `test:projects-workspace:ui` | Exit 0; 9 grouped browser checks |
| `npm test` — isolated copy | Exit 0; 146 checks |
| Bootstrap contract / manifest / executor / verifier | Exit 0; 19 checks / manifest valid / 16 / 20 |
| Lint, build, WBS check, final control QA | Exit 0 |
| `test:complete` — isolated subset | Exit 1; 24 suites passed, 2 pre-existing failures |

The same two broader failures occurred before and after remediation: earlier evidence regeneration causes bootstrap hash rejection. One Git-invoking suite was explicitly blocked. Standalone bootstrap checks pass.

**201 protected files were unchanged**, with no new live-state files. No Git commands, live bootstrap, release or downstream work occurred this session. Human UI review remains pending.

[Full handoff, changed files, exact commands and evidence](C:/Users/HomePC/dev/docmonstakrakin/docs/07_verification/DMK_194_PROJECTS_WORKSPACE_REVIEW.md:103).